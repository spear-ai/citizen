{
  "install": "EXPECTED_SHA256=\"959feedf8541552fa5111f1cd185650c6857404719b4f718e85c0bae5337843b\"; if [[ -n \"${SCRIPT_DOWNLOAD_ROOT_URL:-}\" ]]; then CURL_AUTH=\"\"; [[ -n \"${GITHUB_SCRIPTS_TOKEN:-}\" ]] && CURL_AUTH=\"Authorization: token $GITHUB_SCRIPTS_TOKEN\"; echo \"[Setup] Downloading from: $SCRIPT_DOWNLOAD_ROOT_URL/setup.sh\" && _setup_script=$(curl -fsSL ${CURL_AUTH:+-H \"$CURL_AUTH\"} \"$SCRIPT_DOWNLOAD_ROOT_URL/setup.sh\") && _actual_sha256=$(printf '%s\\n' \"$_setup_script\" | sha256sum | awk '{print $1}') && if [[ \"$_actual_sha256\" != \"$EXPECTED_SHA256\" ]]; then echo \"[Setup] Error: setup.sh checksum mismatch (expected: $EXPECTED_SHA256, got: $_actual_sha256)\" >&2; false; else source /dev/stdin <<< \"$_setup_script\"; fi || echo \"[Setup] Warning: GPG setup failed, continuing without commit signing\"; else echo \"[Setup] SCRIPT_DOWNLOAD_ROOT_URL not set — skipping GPG signing setup\"; fi; source .cursor/scripts/install.sh"
}
