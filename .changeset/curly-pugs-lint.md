---
"@spear-ai/eslint-config": major
---

Support ESLint 10 by updating the TypeScript ESLint stack, removing stale Airbnb-derived config dependencies, and widening the ESLint peer range to include `^10.0.0`.
